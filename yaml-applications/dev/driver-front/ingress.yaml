apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plannify-dev-web-driver-app
  namespace: plannify-dev
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-plannify-dev
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/rewrite-target: /
    acme.cert-manager.io/http01-edit-in-place: "true"
spec:
  ingressClassName: traefik
  rules:
    - host: driver-dev.plannify.be
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plannify-dev-web-driver-app
                port:
                  number: 80
  tls:
    - hosts:
        - driver-dev.plannify.be
      secretName: dev-web-driver-app-plannify-be-tls
