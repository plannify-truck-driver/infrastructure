apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: plannify-dev-web-driver-app
  name: plannify-dev-web-driver-app
  namespace: plannify-dev
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: plannify-dev-web-driver-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: plannify-dev-web-driver-app
    spec:
      containers:
        - image: ghcr.io/plannify-truck-driver/web-driver-app:bdf7ceba9a525b51badc766e35751ce1f8001f8e
          name: plannify-dev-web-driver-app
          imagePullPolicy: Always
          resources:
            requests:
              cpu: "92m"
              memory: "256Mi"
            limits:
              cpu: "128m"
              memory: "512Mi"
          ports:
            - containerPort: 80
              protocol: TCP
          startupProbe:
            httpGet:
              path: /
              port: 80
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            failureThreshold: 3
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /
              port: 80
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 10
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
      imagePullSecrets:
        - name: ghcr-secret
